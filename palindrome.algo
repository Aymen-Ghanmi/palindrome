ALGORITHM algorithm_name
VAR
    i:INTEGER;
    j:INTEGER;
    word:BOOLEAN;
    palindrome:true

BEGIN
    i=0
    j=word.lenth-1
    if (word[i+1]==word[j-1]) and if ([j]<=[i]) return palindrome 
    else result:=word(i, j-1);

END 